# Docker-based SSH Bastion over 

## Setup

This image is used as part of my [localdev Docker Compose config](../localdev/docker-compose.yml)
by building from source.

```bash
docker build -t bastion-tsnet .
```
